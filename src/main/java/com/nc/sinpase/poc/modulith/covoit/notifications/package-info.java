@ApplicationModule(
        displayName = "Notifications — listeners d'événements (mock log)",
        allowedDependencies = {"rides", "bookings"}
)
package com.nc.sinpase.poc.modulith.covoit.notifications;

import org.springframework.modulith.ApplicationModule;
