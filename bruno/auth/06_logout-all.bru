meta {
  name: Logout All Sessions
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/auth/logout-all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
