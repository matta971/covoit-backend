meta {
  name: Get Me
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.email: isDefined
  res.body.displayName: isDefined
}
