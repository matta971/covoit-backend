meta {
  name: Accept Booking Request
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/driver/me/booking-requests/{{bookingRequestId}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
