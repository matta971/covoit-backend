meta {
  name: Cancel Booking (Cleanup)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/booking-requests/{{bookingRequestId}}/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{passengerToken}}
}

assert {
  res.status: eq 204
}
