meta {
  name: Cancel Ride (Cleanup)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/rides/{{rideId}}/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
