meta {
  name: Get Ride By Id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/rides/{{rideId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.rideId: isDefined
  res.body.from: isDefined
  res.body.to: isDefined
}
